// FROZEN: sha256(bookSource:lesson:kanji??kana:kana).slice(0,16) — NEVER CHANGE after user data exists
export function generateVocabId(_bookSource: string, _lessonNumber: number, _kanji: string | null, _kana: string): string {
  throw new Error('FROZEN ALGORITHM — implement in spec-002')
}
